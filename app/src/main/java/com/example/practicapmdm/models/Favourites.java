package com.example.practicapmdm.models;

public class Favourites {
    private Pool pool;

    public Favourites(Pool pool) {
        this.pool = pool;
    }

    public Pool getPool() {
        return pool;
    }

    public void setPool(Pool pool) {
        this.pool = pool;
    }
}
